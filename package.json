{
  "name": "investpro",
  "version": "1.0.0",
  "description": "InvestPro — Portefeuille BRVM",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}